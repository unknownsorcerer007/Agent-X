# Agent-X Requirements — Production Edition
# Core browser automation
patchright>=1.50.0
playwright>=1.60.0

# WebSocket server for agent connections
websockets>=13.1
aiohttp>=3.10.0
aiohttp-cors>=0.7.0

# HTTP client (used by connectors)
httpx>=0.27.0

# Security & encryption
cryptography>=42.0.0
PyJWT>=2.7.0
bcrypt>=4.0.0
passlib>=1.7.4

# Anti-detection evasion engines
curl_cffi>=0.15.0         # curl-impersonate: browser-grade TLS fingerprint
cloudscraper>=1.2.71       # Cloudflare solver

# Bug bounty / scanning tools
beautifulsoup4>=4.12.0
lxml>=5.2.0

# Configuration
pyyaml>=6.0.0

# Utilities
requests>=2.31.0
urllib3>=2.0.0,<3.0.0      # Modern urllib3 (v2 is stable, fixes many issues)
psutil>=5.9.0

# Human mimicry
numpy>=1.24.0

# MCP protocol support
mcp>=1.5.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ─── Production Infrastructure ───────────────────────────────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.20.0
alembic>=1.13.0
redis>=5.0.0
pydantic>=2.0.0
structlog>=24.1.0

# ─── Swarm & AI ──────────────────────────────────────────────
openai>=1.20.0
anthropic>=0.25.0
# ddddocr>=1.4.0            # Optional: CAPTCHA solver (install manually if needed)
pytesseract>=0.3.10
Pillow>=10.0.0
tiktoken>=0.7.0
trafilatura>=1.8.0
readability-lxml>=0.8.1
# DrissionPage>=4.0.0       # Optional: Alternative browser engine
# whisper-openai>=20231117  # Optional: Audio transcription

# ─── Server & API ────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.27.0
sse-starlette>=2.0.0

# ─── Cloud & AWS (Optional) ─────────────────────────────────
# boto3>=1.34.0             # Optional: AWS Bedrock support
# botocore>=1.34.0          # Optional: AWS Bedrock support
